#!/usr/bin/env bash
# Launch Element Desktop with the dod profile.
set -euo pipefail

if ! command -v element-desktop >/dev/null 2>&1; then
  printf 'element-desktop is not installed or not on PATH.\n' >&2
  exit 1
fi

exec element-desktop --profile dod "$@"
