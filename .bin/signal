#!/usr/bin/env bash
# Launch Signal Desktop minimized to tray when available.
set -euo pipefail

if ! command -v signal-desktop >/dev/null 2>&1; then
  printf 'signal-desktop is not installed or not on PATH.\n' >&2
  exit 1
fi

exec signal-desktop --start-in-tray "$@"
